# Profile: local — activated via SPRING_PROFILES_ACTIVE=local in .env or docker-compose.
# Configuration specific to local development (IntelliJ + Docker dependencies).
# Values here override application.properties when this profile is active.

spring.jpa.hibernate.ddl-auto=create-drop
app.db.init=all

app.cors.allowed-origin=${CORS_ALLOWED_ORIGIN:https://localhost:4202}
