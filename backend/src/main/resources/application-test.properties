# In test mode, Spring firstly reads application.properties, and then only overrides these variables with the values in this file

server.ssl.enabled=false

# Test DB Connections
spring.datasource.url=jdbc:mysql://localhost:3307/${RUNNER_MYSQL_NAME:FrictTest}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=${RUNNER_MYSQL_PASSWORD:password}

spring.data.mongodb.uri=mongodb://${RUNNER_MONGODB_USERNAME:root}:${RUNNER_MONGODB_PASSWORD:password}@localhost:27018/${RUNNER_MONGODB_NAME:FrictTest}?authSource=admin

# Create objects and clean DB at the end
spring.jpa.hibernate.ddl-auto=create-drop

# Test JWT and cards DB keys
jwt.secret=6B59703373367639792F423F4528482B4D6251655468576D5A7134743777217A
app.db-encrytion.key=ClaveSecreta1234

app.db.init=none

# MinIO test container
app.storage.endpoint=http://localhost:9002
app.storage.public-url=http://localhost:9002/${RUNNER_MINIO_BUCKET_NAME:images}
app.storage.bucket-name=${RUNNER_MINIO_BUCKET_NAME:images}
app.storage.region=us-east-1
AWS_ACCESS_KEY_ID=root
AWS_SECRET_ACCESS_KEY=password

# CORS (tests run against a local backend, no cross-origin involved)
app.cors.allowed-origin=http://localhost:4202

# Test mail and auth servers connection
spring.mail.port=587
spring.mail.username=test@localhost
spring.mail.password=dummy
google.auth.clientId=test-client-id
