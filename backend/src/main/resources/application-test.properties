# In test mode, Spring firstly reads application.properties, and then only overrides these variables with the values in this file

# Test DB Connections
spring.datasource.url=jdbc:mysql://localhost:3306/${RUNNER_DB_NAME:FrictTest}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=${RUNNER_DB_PASSWORD:p4ssw0rd}

spring.data.mongodb.uri=mongodb://admin:password123@localhost:27017/${RUNNER_MONGODB_NAME:FrictTest}?authSource=admin

# Create objects and clean DB at the end
spring.jpa.hibernate.ddl-auto=create-drop

# Test JWT and cards DB keys
jwt.secret=6B59703373367639792F423F4528482B4D6251655468576D5A7134743777217A
app.cards-db.key=ClaveSecreta1234

app.db.init=none

# MinIO empty test container (tests execution only)
minio.url=https://localhost:9000
minio.public-url=https://localhost:9000
minio.access-key=${RUNNER_MINIO_USER:admin}
minio.secret-key=${RUNNER_MINIO_PASSWORD:password123}
minio.bucket-name=${RUNNER_MINIO_BUCKET_NAME:images}
minio.ssl.insecure=true

# Test mail and auth servers connection
spring.mail.port=587
spring.mail.username=test@localhost
spring.mail.password=dummy
google.auth.clientId=test-client-id